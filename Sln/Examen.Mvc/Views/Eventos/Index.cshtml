@model IEnumerable<Examen.Models.Eventos>
@using Microsoft.AspNet.Identity;


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>


    @{var estaAutenticado = User.Identity.IsAuthenticated;
        if (estaAutenticado)
        {
            var NombreUsuario = User.Identity.Name;

            if (NombreUsuario == "admin@events.com")
            {
                
            }
            else
            {
                @Html.ActionLink("Create New", "Create")
            }
        }}

</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_inicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Autor)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.Tiempo_Inicio)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Duracion_Horas)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Ubicacion)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Autor)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Tiempo_Inicio)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Duracion_Horas)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Ubicacion)
            </td>
            <td>



                @{
                    if (estaAutenticado)
                    {
                        var NombreUsuario = User.Identity.Name;

                        if (NombreUsuario == "admin@events.com")
                        {
                            @Html.ActionLink("Edit ", "Edit", new { id = item.Id })
                            @Html.ActionLink("Delete ", "Delete", new { id = item.Id }, new { onclick = "return confirm('Esta Seguro de eliminar de la lista?');" })
                        }
                        else
                        {
                            @Html.ActionLink("Edit ", "Edit", new { id = item.Id })
                            @Html.ActionLink("Delete ", "Delete", new { id = item.Id }, new { onclick = "return confirm('Esta Seguro de eliminar de la lista?');" })
                            @Html.ActionLink("ComentarioByEventos ", "ComentarioByEventos", new { id = item.Id })

                        }
                    }
                    else
                    {

                    }
                }






            </td>
        </tr>
    }

</table>
